#include "libft.h"

void	*ft_memchr(const void *arr, int c, size_t len)
{
	char 	*arr1;
	size_t	i;

	arr1 = (char*)arr;
	i = 0;
	if (!arr1[i])
		return (NULL);
	else
		while (arr1[i] && i < len)
		{
			
		} 
}